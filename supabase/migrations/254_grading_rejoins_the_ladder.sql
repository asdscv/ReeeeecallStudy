-- 254: 서술형 채점 $0.40 → $0.10, 주관식 채점 $0.10 → $0.05.
--
-- 카드를 $0.01 로 내린 뒤(249) 사다리가 다시 어긋났습니다. 지금 값을 실측 원가와 나란히 놓으면
-- 이렇습니다:
--
--       카드 1장        $0.01     원가 $0.00028      36배
--       객관식 문항      $0.10     원가 $0.00048     208배
--       주관식 채점      $0.10     원가 $0.00042     238배
--       서술형 채점      $0.40     원가 $0.00032    1233배   ← 혼자 다른 세상
--
-- 서술형 채점 원가는 앱과 같은 프롬프트·모델로 실제 세 건을 채점해 잰 값입니다(입력 516 /
-- 출력 130 토큰 평균). 서술형이 **생성**은 비싸도 **채점**은 비싸지 않습니다 — 루브릭은 이미
-- 문항에 저장돼 있고, 모델은 기준별 등급만 돌려줍니다. 8유닛은 그 사실이 알려지기 전의 값입니다.
--
-- 답안 하나에 $0.40 은 학습자가 서술형을 한 번 풀고 그만두게 만드는 값입니다. 안 눌린 $0.40 은
-- $0 이고, 이 앱에서 서술형은 가장 많이 배우는 유형입니다.
--
-- 새 값에서도 바닥은 넉넉합니다: $0.10 은 원가의 300배쯤이고 `price-floor.test.ts` 가 요구하는
-- 10배에서 한참 위입니다.
--
-- 230 의 규율대로 절대값을 옛 값으로 가드합니다.
BEGIN;

UPDATE public.ai_quiz_price_units SET units = 2, updated_at = now()
 WHERE action = 'grade_essay' AND units = 8;
UPDATE public.ai_quiz_price_units SET units = 1, updated_at = now()
 WHERE action = 'grade_short' AND units = 2;

COMMIT;
