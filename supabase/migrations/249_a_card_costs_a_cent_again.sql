-- 249: AI 카드 한 장 $0.10 → $0.01.
--
-- 230 이 모든 값을 열 배로 올렸고, 카드는 그때 $0.01 → $0.10 이 됐습니다. 소유자의 판단으로
-- 카드만 되돌립니다.
--
-- 근거가 되는 숫자는 이렇습니다. 프로덕션 원장에서 텍스트 카드 생성 35콜의 평균 원가가
-- $0.00106 이고 그 콜들이 만든 카드가 132장이니, **장당 원가는 약 $0.00028** 입니다.
-- $0.10 은 그 350배쯤이고, $0.01 이면 36배입니다. `price-floor.test.ts` 가 요구하는 바닥은
-- 10배이므로 한참 위입니다 — 실제로 그 테스트의 PRICE 표에는 카드가 아직 10,000 으로
-- 적혀 있습니다. 230 이후로 갱신되지 않은 것이고, 이 변경이 그 숫자와 다시 맞춥니다.
--
-- 학습자 쪽에서 보면 이렇습니다: 하루 무료 10장을 다 쓰고 25장을 더 만들면 $2.50 이었던
-- 것이 $0.25 가 됩니다.
--
-- 실측된 이미지 덱 원가와도 자리가 맞습니다. 실제로 재 보니(1600px JPEG, gemini-3.1-flash-lite,
-- 앱과 같은 프롬프트) 사진 1장 $0.00133 · 4장 $0.00216 · 8장 $0.00325 이고 한 번에 카드
-- 10장이 나왔습니다. 이미지는 호출당 $0.10 고정이니 장당 $0.01 — 카드 한 장 값과 같습니다.
-- 두 경로가 같은 물건을 같은 값에 팝니다.
--
-- 230 의 규율대로 절대값을 옛 값으로 가드합니다. 곱셈은 멱등이 아니고 마이그레이션은 정확히
-- 한 번 실행되지 않습니다.
BEGIN;

UPDATE public.ai_action_prices
   SET price_micro = 10000, updated_at = now()
 WHERE action = 'card' AND price_micro = 100000;

COMMIT;
