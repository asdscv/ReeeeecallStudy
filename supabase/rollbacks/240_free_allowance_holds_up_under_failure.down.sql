-- Down for 240: 239 시점의 세 함수로 되돌립니다.
--
-- 되돌리면 239의 결함 셋이 함께 돌아옵니다 — 실패한 생성이 그날 무료 문항을 태우고,
-- `unit_kind='unit'`에서 정산이 예약보다 비싸지고, 예약과 정산의 잠금 순서가 어긋납니다.
-- 그게 이 롤백이 되돌아가는 상태이지 새로 생기는 결함은 아닙니다.
--
-- 239 파일이 세 함수를 통째로 CREATE OR REPLACE 하므로, 그대로 다시 실행하면 됩니다:
--
--     psql -f supabase/migrations/239_free_allowance_is_data.sql
--
-- 여기서 본문을 복사해 두면 두 벌이 되고, 다음에 하나만 고쳐집니다.
